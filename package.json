{
  "name": "simple-ocr-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prepare:web": "node scripts/prepare-web.mjs",
    "dev": "npm run prepare:web && tauri dev",
    "build": "npm run prepare:web && tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.5"
  }
}
